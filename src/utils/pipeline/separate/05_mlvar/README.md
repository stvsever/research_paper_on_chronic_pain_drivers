# Stage 05 - mlVAR benchmark

The multilevel VAR pooled benchmark on the four core nodes, plus residual diagnostics. This is the partial-pooling reference against which the person-specific networks are read.
